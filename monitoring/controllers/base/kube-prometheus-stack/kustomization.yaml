apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - repository.yaml
  - release.yaml
  # Replaces the chart's KubeHpaMaxedOut, which release.yaml disables.
  - hpa-maxedout-rule.yaml
