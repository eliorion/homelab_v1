apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Applied after the chart: infra-linstor-monitoring dependsOn monitoring-controllers,
# so the ServiceMonitor and PrometheusRule CRDs exist.
resources:
  - servicemonitor.yaml
  - prometheusrule.yaml
