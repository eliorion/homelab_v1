apiVersion: v1
kind: Namespace
metadata:
  name: identity
---
# This component owns the `identity` Namespace; `infrastructure/services/base/
# keycloak` must NOT declare it too, or two Flux Kustomizations fight over it.
