apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Applied after the chart: infra-seaweedfs-monitoring dependsOn monitoring-controllers,
# so the PrometheusRule CRD exists.
resources:
  - prometheusrule.yaml
