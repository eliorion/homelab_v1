apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cnpg/
  # Tailscale operator — exposes the asp/fbref admin UIs on the tailnet.
  - tailscale-operator/
  # CephCluster. Its HelmRelease is suspended — unsuspending wipes four disks.
  - rook-ceph-cluster/
