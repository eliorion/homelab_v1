apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - repository.yaml
  - release.yaml
  # Decrypted by the infrastructure-controllers Flux Kustomization
  # (it has decryption.provider: sops).
  - operator-oauth.enc.yaml
  - egress-proxies.yaml
