apiVersion: v1
kind: Secret
metadata:
  name: seaweedfs-s3-config
  namespace: seaweedfs
type: Opaque
stringData:
  seaweedfs_s3_config: |
    {
      "identities": [
        {
          "name": "nextcloud",
          "credentials": [
            {"accessKey": "REPLACE_ME", "secretKey": "REPLACE_ME"}
          ],
          "actions": ["Read:nextcloud", "Write:nextcloud", "List:nextcloud", "Tagging:nextcloud"]
        },
        {
          "name": "tmp-backup-garage",
          "credentials": [
            {"accessKey": "REPLACE_ME", "secretKey": "REPLACE_ME"}
          ],
          "actions": ["Read:tmp-backup-garage", "Write:tmp-backup-garage", "List:tmp-backup-garage", "Tagging:tmp-backup-garage"]
        }
      ]
    }
