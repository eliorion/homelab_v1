apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/databases/ai-gateway
  - ai-gateway-garage-backup-credentials.enc.yaml
  - objectstore.yaml
  - scheduledbackup.yaml

patches:
  # TEMPORARY restore from the Barman object store -- see the patch file.
  - path: cluster-recovery-patch.yaml
    target:
      kind: Cluster
      name: ai-gateway-db
  - path: cluster-backup-patch.yaml
    target:
      kind: Cluster
      name: ai-gateway-db
