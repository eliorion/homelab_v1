apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: identity

resources:
  - ../../../base/databases/keycloak
  - r2-backup-credentials.enc.yaml
  - objectstore.yaml
  - scheduledbackup.yaml

patches:
  - path: cluster-backup-patch.yaml
    target:
      kind: Cluster
      name: keycloak-db
