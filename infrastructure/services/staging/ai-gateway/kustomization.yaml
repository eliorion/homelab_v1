apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base/ai-gateway
  # Decrypted by the `infrastructure-services` Flux Kustomization (sops-age).
  # Encrypted manifests live only in overlays — never in base/.
  - ai-gateway-secrets.enc.yaml
# ai-gateway-db itself moved to ../databases/ai-gateway/ — every infra/services
# database now lives together there, mirroring apps/staging/databases/.
