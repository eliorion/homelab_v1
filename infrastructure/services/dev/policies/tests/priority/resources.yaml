apiVersion: v1
kind: Pod
metadata:
  name: app
  namespace: preview-pr-12
spec:
  priorityClassName: platform-critical
  priority: 100000
  preemptionPolicy: PreemptLowerPriority
  containers:
  - name: app
    image: nginx
