apiVersion: v1
kind: Namespace
metadata:
  name: ai-gateway
  labels:
    # Selectable by NetworkPolicies in consumer namespaces (agent pods egress
    # to the gateway by namespace label, never by pod IP).
    name: ai-gateway
