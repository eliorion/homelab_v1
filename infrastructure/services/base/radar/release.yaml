apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: radar
  namespace: flux-system
spec:
  targetNamespace: radar
  releaseName: radar
  interval: 30m
  chart:
    spec:
      # Renovate keeps this pinned version current.
      chart: radar
      version: "1.8.6"
      sourceRef:
        kind: HelmRepository
        name: skyhook
        namespace: flux-system
      interval: 12h
  install:
    createNamespace: true
  values:
    fullnameOverride: radar
    replicaCount: 1
    service:
      type: ClusterIP
      port: 9280
      # Publish the Radar UI (and /mcp) on the tailnet via the Tailscale
      # operator -- authed by tailnet identity, off-LAN, never internet-exposed
      # (same pattern as the Longhorn / asp / fbref admin UIs). Reach it at
      # http://radar.<your-tailnet>.ts.net (plain HTTP, not 443).
      annotations:
        tailscale.com/expose: "true"
        tailscale.com/hostname: radar
    mcp:
      enabled: true
    # Read-only posture: view everything, mutate nothing. GitOps cluster --
    # never change resources by hand, so Radar needs no write verbs.
    rbac:
      create: true
      helm: false
      podExec: false
      portForward: false
      secrets: false
    resources:
      requests:
        cpu: 50m
        memory: 128Mi
      limits:
        cpu: "1"
        memory: 512Mi
