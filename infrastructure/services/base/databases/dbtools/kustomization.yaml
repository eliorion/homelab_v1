apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The `database` namespace itself is owned by the workload dir
# (../../dbtools/namespace.yaml), mirroring ai-gateway: the service creates its
# namespace, this dir adds only its database.
resources:
  - cluster.yaml
  - pgadmin-database.yaml
