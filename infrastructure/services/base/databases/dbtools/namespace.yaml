# Database-tooling namespace: pgAdmin, nao and postgres-mcp. See README.md.
apiVersion: v1
kind: Namespace
metadata:
  name: database
  labels:
    # Selectable by NetworkPolicies in other namespaces — do not rename. (The
    # reflector allow-lists match the namespace NAME, not this label.)
    name: database
