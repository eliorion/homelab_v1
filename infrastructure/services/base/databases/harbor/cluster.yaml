apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: harbor-db
spec:
  instances: 2
  bootstrap:
    initdb:
      # Must match database.external.coreDatabase in the Harbor HelmRelease.
      database: harbor
      owner: harbor
  storage:
    size: 20Gi
