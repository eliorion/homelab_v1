apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nexus
  namespace: flux-system
spec:
  targetNamespace: nexus
  releaseName: nexus
  interval: 30m
  timeout: 10m
  chart:
    spec:
      chart: nexus3
      version: "5.22.0"
      sourceRef:
        kind: HelmRepository
        name: stevehipwell
        namespace: flux-system
      interval: 12h
  install:
    createNamespace: true
  # Without an upgrade block, remediation.retries defaults to 0 -- and 0 means
  # helm-controller marks the release Stalled after ONE failure and stops
  # retrying entirely. That is what happened here: a single failed upgrade on
  # 2026-07-27 latched Stalled=True reason=RetriesExceeded, and the release sat
  # Ready=False for over two weeks with nothing retrying and nothing alerting
  # loudly enough to be noticed.
  #
  # This does NOT by itself fix the current failure -- see the note on
  # persistence.size below; that needs a one-off orphan-delete of the
  # StatefulSet. What it fixes is the latch, so the NEXT transient failure
  # (registry blip, slow boot past the 10m timeout) retries and self-heals
  # instead of parking the CI cache indefinitely.
  #
  # Safe to add while the release is broken: this changes spec, not
  # spec.values, so the rendered pod template is byte-identical. The
  # StatefulSet's ControllerRevision hash is unchanged and nexus-0 is adopted
  # in place rather than rolled -- which matters, because rolling it means a
  # cold cache and a Longhorn re-attach of a 267 GiB volume.
  upgrade:
    remediation:
      retries: 3
      strategy: rollback
  values:
    # Keep the service named `nexus` so cluster DNS stays nexus.nexus.svc:8081.
    fullnameOverride: nexus
    persistence:
      enabled: true
      # ONE replica on SSD: this is a rebuildable proxy cache, so redundancy is
      # not worth the space and DRBD would double it. The node holding it going
      # down takes Nexus down until it returns.
      #
      # 30Gi: the docker formats moved to Harbor 2026-09-16, so this holds only
      # the PyPI/maven/nuget proxies and Nexus's own config database. node-1 is
      # deliberate -- it is the NVMe box and the fastest storage in the cluster.
      #
      # storageClass and size are immutable once the StatefulSet exists —
      # changing either means deleting the StatefulSet and migrating the PVC.
      storageClass: ssd-single
      size: 30Gi
    # Raise -Xmx and resources.limits.memory together, never one alone.
    install4jAddVmParams: "-Xms1g -Xmx2g -XX:MaxDirectMemorySize=2048m"
    resources:
      requests:
        cpu: 500m
        memory: 2Gi
      limits:
        cpu: 10
        memory: 6Gi
    # Soft preference for node staging-controlplane-1.
    affinity:
      nodeAffinity:
        preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            preference:
              matchExpressions:
                - key: kubernetes.io/hostname
                  operator: In
                  values:
                    - staging-controlplane-1
    rootPassword:
      # SOPS-encrypted secret provided by the staging overlay.
      secret: nexus-root-password
      key: password
    service:
      additionalPorts:
        - port: 5000
          name: docker-hub
          containerPort: 5000
        - port: 5001
          name: docker-ghcr
          containerPort: 5001
    config:
      enabled: true
      anonymous:
        enabled: true
      # Docker clients authenticate via token — required even for anonymous pull.
      realms:
        enabled: true
        values:
          - NexusAuthenticatingRealm
          - DockerToken
      cleanup:
        - name: purge-stale
          notes: "Drop anything not downloaded in 14 days."
          format: ALL_FORMATS
          mode: delete
          criteria:
            lastDownloaded: "1209600"
      repos:
        - name: pypi-proxy
          format: pypi
          type: proxy
          online: true
          storage:
            blobStoreName: default
            strictContentTypeValidation: true
          proxy:
            remoteUrl: https://pypi.org
            contentMaxAge: 1440
            metadataMaxAge: 1440
          negativeCache:
            enabled: true
            timeToLive: 1440
          httpClient:
            blocked: false
            autoBlock: true
          cleanup:
            policyNames: [purge-stale]
        - name: docker-hub
          format: docker
          type: proxy
          online: true
          storage:
            blobStoreName: default
            strictContentTypeValidation: true
          proxy:
            remoteUrl: https://registry-1.docker.io
            contentMaxAge: 1440
            metadataMaxAge: 1440
          negativeCache:
            enabled: true
            timeToLive: 1440
          httpClient:
            blocked: false
            autoBlock: true
          docker:
            v1Enabled: false
            forceBasicAuth: false # anonymous pull
            httpPort: 5000
          dockerProxy:
            indexType: HUB
          cleanup:
            policyNames: [purge-stale]
        - name: ghcr
          format: docker
          type: proxy
          online: true
          storage:
            blobStoreName: default
            strictContentTypeValidation: true
          proxy:
            remoteUrl: https://ghcr.io
            contentMaxAge: 1440
            metadataMaxAge: 1440
          negativeCache:
            enabled: true
            timeToLive: 1440
          httpClient:
            blocked: false
            autoBlock: true
          docker:
            v1Enabled: false
            forceBasicAuth: false
            httpPort: 5001
          dockerProxy:
            indexType: REGISTRY
          cleanup:
            policyNames: [purge-stale]
