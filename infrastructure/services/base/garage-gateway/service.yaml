apiVersion: v1
kind: Service
metadata:
  name: garage-s3
  namespace: garage-gw
  labels:
    app: garage-gateway
spec:
  type: ClusterIP
  selector:
    app: garage-gateway
  ports:
    - name: s3
      port: 3900
      targetPort: 3900
