apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - release.yaml
  # advisor-pipeline's ai-gateway virtual key (WP2.4). Uncomment once the file exists:
  # - advisor-ai-gateway-token.enc.yaml
