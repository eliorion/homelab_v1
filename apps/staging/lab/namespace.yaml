# Central analysis-sandbox namespace. No PSA opt-out label: the lab pods must stay
# non-root / read-only-rootfs / drop-ALL-caps to pass the cluster-default baseline.
apiVersion: v1
kind: Namespace
metadata:
  name: lab
