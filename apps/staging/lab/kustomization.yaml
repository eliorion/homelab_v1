apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The lab ns + the single lab HelmRelease. Reflect-stub Secrets are generated by
# the chart — do not add them here.
resources:
  - namespace.yaml
  - release.yaml
