apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The lab ns + the single lab HelmRelease (one pod per project in its `projects`
# list). The reflect-stub Secrets are generated by the chart now — not here.
resources:
  - namespace.yaml
  - release.yaml
