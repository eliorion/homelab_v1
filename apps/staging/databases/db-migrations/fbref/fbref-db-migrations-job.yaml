# Flyway migration Job for the fbref database.
# NOTE: no ttlSecondsAfterFinished — Flux owns the Job lifecycle (force).
# A TTL would delete the completed Job and make every later reconcile
# re-create + re-run it for nothing.
apiVersion: batch/v1
kind: Job
metadata:
  name: fbref-db-migrate
  namespace: fbref
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app.kubernetes.io/name: fbref-db-migrations
    spec:
      restartPolicy: Never
      imagePullSecrets:
        - name: ghcr-pull-secret
      securityContext:
        runAsNonRoot: true
        runAsUser: 101
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: flyway
          image: ghcr.io/eliorion/fbref-db-migrations:fbref-db-migrations-v0.3.0
          args: ["migrate"]
          env:
            # jdbc-uri must resolve to the bootstrap database "fbref"
            # (bootstrap.initdb.database in apps/base/databases/fbref/database.yaml)
            - name: FLYWAY_URL
              valueFrom:
                secretKeyRef:
                  name: fbref-db-app
                  key: jdbc-uri
            - name: FLYWAY_USER
              valueFrom:
                secretKeyRef:
                  name: fbref-db-app
                  key: username
            - name: FLYWAY_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: fbref-db-app
                  key: password
            - name: FLYWAY_BASELINE_ON_MIGRATE
              value: "true"
            - name: FLYWAY_BASELINE_VERSION
              value: "0"
            - name: FLYWAY_CONNECT_RETRIES
              value: "10"
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false # flyway writes temp files under /flyway
            capabilities:
              drop: ["ALL"]
