apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# db-migrations/ is intentionally NOT listed — it is applied by its own
# Flux Kustomization, which dependsOn this one.
resources:
  - asp/
  - fbref/
  - n8n/
  - scraper/
