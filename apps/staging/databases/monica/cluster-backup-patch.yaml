apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: monica-db
spec:
  plugins:
    - name: barman-cloud.cloudnative-pg.io
      isWALArchiver: true
      parameters:
        barmanObjectName: r2-store
        serverName: monica-db
