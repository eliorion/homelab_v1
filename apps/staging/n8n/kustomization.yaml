apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: n8n

# n8n-secrets.enc.yaml does NOT ship in git — create it from the .example
# (see its header) before the first push.
resources:
  - ../../base/n8n
  - configmap.yaml
  - n8n-secrets.enc.yaml
