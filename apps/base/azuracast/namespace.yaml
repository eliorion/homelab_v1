# No pod-security labels on purpose. Do not add `enforce: restricted` here: the
# all-in-one image runs supervisord as root and the pod will not start.
apiVersion: v1
kind: Namespace
metadata:
  name: azuracast
