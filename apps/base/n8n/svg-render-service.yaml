apiVersion: v1
kind: Service
metadata:
  name: svg-render
  namespace: n8n
  labels:
    app: svg-render
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    app: svg-render
