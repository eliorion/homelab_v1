apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The `n8n` namespace and the n8n-db CNPG cluster ship from the databases tier
# (apps/base/databases/n8n), which Flux reconciles first.
resources:
  - storage.yaml
  - deployment.yaml
  - service.yaml
  - ingress-tailscale.yaml
