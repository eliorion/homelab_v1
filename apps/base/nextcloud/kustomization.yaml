apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The `nextcloud` namespace and the nextcloud-db CNPG cluster ship from the
# databases tier.
resources:
  - storage.yaml
  - valkey.yaml
  - deployment.yaml
  - service.yaml
