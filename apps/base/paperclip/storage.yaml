apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: paperclip-data-pvc
  namespace: paperclip
spec:
  storageClassName: ssd
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
