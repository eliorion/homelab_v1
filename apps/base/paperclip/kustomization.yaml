apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The `paperclip` namespace and the paperclip-db CNPG cluster ship from the databases tier.
resources:
  - storage.yaml
  - deployment.yaml
  - service.yaml
  - ingress-tailscale.yaml
