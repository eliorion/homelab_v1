# TEMPORARY seed-from-staging — points at the STAGING bucket; remove after
# seeding (plan Step 6). Prod archiving stays on objectstore.yaml (r2-store).
apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: r2-store-staging
spec:
  retentionPolicy: "7d"
  configuration:
    destinationPath: "s3://asp-cnpg-staging"
    endpointURL: "https://07e577de68147de704bb467debe46e21.r2.cloudflarestorage.com"
    s3Credentials:
      accessKeyId:
        name: r2-staging-credentials
        key: ACCESS_KEY_ID
      secretAccessKey:
        name: r2-staging-credentials
        key: ACCESS_KEY_SECRET
    wal:
      compression: gzip
      encryption: AES256
    data:
      compression: gzip
      encryption: AES256
      jobs: 2
  instanceSidecarConfiguration:
    # Required for Cloudflare R2: without both, backup AND restore fail with
    # XAmzContentSHA256Mismatch (plugin-barman-cloud #411).
    env:
      - name: AWS_REQUEST_CHECKSUM_CALCULATION
        value: when_required
      - name: AWS_RESPONSE_CHECKSUM_VALIDATION
        value: when_required
